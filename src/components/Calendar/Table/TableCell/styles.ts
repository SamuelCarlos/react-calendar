import styled from '@emotion/styled'

export const Button = styled.button`
  width: 100%;
  height: 100%;

  border: none;
  background-color: transparent;
  cursor: pointer;

  .reminders-box {
    :hover {
      overflow: auto;
    }
  }
`
